# =============================================================================
# MotorShop — variáveis do FRONTEND
#
# Copie para `frontend/.env`.
#
# ⚠️  TUDO neste arquivo é embutido no bundle e é PÚBLICO.
#     Nunca coloque segredo aqui. Segredos ficam no `.env` da raiz, que serve
#     o backend e nunca é lido pelo Vite.
# =============================================================================

VITE_API_URL=http://localhost:3000/api
